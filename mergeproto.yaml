main_proto: some_dir/some_proto.proto
additional_message:
  - some_package.some_message
additional_enum:
  - some_package.some_enum
omit_package_name:
  - some_package
  - some_package.some_sub_package